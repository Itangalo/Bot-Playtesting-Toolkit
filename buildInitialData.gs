/**
 * Builds the data needed to start a game.
 * 
 * Should populate the global and the initialGameState objects.
 * Any decks put in initialGameState.decks will be created on game start.
 */
function buildInitialData() {
  
}
